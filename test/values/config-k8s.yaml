replicaCount: 1

image:
  repository: curlimages/curl
  tag: latest
  pullPolicy: Always

imagePullSecrets: 
  - name: privateregsitry

serviceAccount:
  create: false
  name: "azure-devops-svc"

service:
  type: ClusterIP
  port: 8080
  targetPort: 1880

ingress:
  enabled: true
  annotations:
  kubernetes.io/ingress.class: nginx
  hosts:
    - host: sample.app.domain
      paths: ["/"]
